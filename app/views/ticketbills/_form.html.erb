<%= form_for(@ticketbill) do |f| %>
  <% if @ticketbill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticketbill.errors.count, "error") %> prohibited this ticketbill from being saved:</h2>

      <ul>
      <% @ticketbill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_ticket %><br>
    <%= f.select :id_ticket, options_for_select(Ticket.all.map{|s|[s.info,s.id]}) %>
  </div>
  <div class="field" style="display: none;">
    <%= f.label :Customer %><br>
    <%= f.text_field :id_customer, :value => current_user.name, :readonly => true %>
  </div>
  <div class="field">
    <%= f.label :itinerary_id %><br>
    <%= f.select :itinerary_id, options_for_select(Itinerary.all.map{|s|[s.info,s.id]}) %>  </div>
  <div class="field">
    <%= f.label :departure_date %><br>
    <%= f.date_select :departure_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
