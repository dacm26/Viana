<div lass="container">
<div class="row centered">
	<div class="col-lg-8 col-lg-offset-2">
<%= form_for(@package) do |f| %>
  <% if @package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% @package.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
    <strong>Remember: Price = weight*$35</strong>
  </div>
    <br>
    <br>
  <div class="field">
    <%= f.label :Origin_Customer %><br>
    <%= f.select :origin_customer, options_for_select(User.all.map{|s|[s.name,s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :Destiny_customer %><br>
    <%= f.text_field :destiny_customer%>
  </div>
  <div class="field">
    <%= f.label :origin_city %><br>
    <%= f.select :origin_city, options_for_select(City.all.map{|s|[s.name,s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :destiny_city %><br>
    <%= f.select :destiny_city, options_for_select(City.all.map{|s|[s.name,s.id]}) %>
  </div>
  <div class="field">
    <%= f.label :weight %><br>
    <%= f.text_field :weight %>
  </div>

  <div class="actions">
    <%= f.submit 'Add Package'%>
  </div>
<% end %>
        </div>
  </div><!-- row -->
</div>
