<%= form_for(@package) do |f| %>
  <% if @package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% @package.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :weight %><br>
    <%= f.text_field :weight %>
  </div>
  <div class="field">
    <%= f.label :origin_customer %><br>
    <%= f.select :origin_customer, options_for_select(User.all.map{|s|[s.name,s.email]}) %>
  </div>
  <div class="field">
    <%= f.label :destiny_customer %><br>
    <%= f.text_field :destiny_customer%>
  </div>
  <div class="field">
    <%= f.label :id_itinerary %><br>
    <%= f.select :id_itinerary, options_for_select(Itinerary.all.map{|s|[s.info,s.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
