<%= form_for(@customercategory) do |f| %>
  <% if @customercategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customercategory.errors.count, "error") %> prohibited this customercategory from being saved:</h2>

      <ul>
      <% @customercategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
